import streamlit as st

def run_copy_generator(
    tool_name="Copy Generator",
    sidebar_info=None,
    left_text_label="Notes",
    right_box_labels=None,
    box_values=None,
):
    # This framework is now only used as a template
    # The actual copy generator tool handles everything directly
    st.header(tool_name)
    st.info("This framework has been replaced by the enhanced copy generator.")
    return None, ""